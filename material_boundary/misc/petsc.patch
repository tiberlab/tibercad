--- src/snes/impls/ls/ls.c.orig	2008-05-09 17:15:53.000000000 +0200
+++ src/snes/impls/ls/ls.c	2008-05-09 17:15:53.000000000 +0200
@@ -186,7 +186,7 @@
     if (kspreason < 0) {
       if (++snes->numLinearSolveFailures >= snes->maxLinearSolveFailures) {
         snes->reason = SNES_DIVERGED_LINEAR_SOLVE;
-        break;
+        PetscFunctionReturn(0);
       }
     }
     ierr = KSPGetIterationNumber(snes->ksp,&lits);CHKERRQ(ierr);
@@ -675,9 +675,9 @@
       CHKERRQ(ierr);
       ierr = VecNormBegin(g,NORM_2,gnorm);CHKERRQ(ierr);
       if (*gnorm != *gnorm) SETERRQ(PETSC_ERR_FP,"User provided compute function generated a Not-a-Number");
-      ierr = VecNormBegin(w,NORM_2,ynorm);CHKERRQ(ierr);
+      ierr = VecNormBegin(y,NORM_2,ynorm);CHKERRQ(ierr);
       ierr = VecNormEnd(g,NORM_2,gnorm);CHKERRQ(ierr);
-      ierr = VecNormEnd(w,NORM_2,ynorm);CHKERRQ(ierr);
+      ierr = VecNormEnd(y,NORM_2,ynorm);CHKERRQ(ierr);
     }
   }
   ierr = PetscLogEventEnd(SNES_LineSearch,snes,x,f,g);CHKERRQ(ierr);
@@ -848,9 +848,9 @@
       }
       CHKERRQ(ierr);
       ierr = VecNormBegin(g,NORM_2,gnorm);CHKERRQ(ierr);
-      ierr = VecNormBegin(w,NORM_2,ynorm);CHKERRQ(ierr);
+      ierr = VecNormBegin(y,NORM_2,ynorm);CHKERRQ(ierr);
       ierr = VecNormEnd(g,NORM_2,gnorm);CHKERRQ(ierr);
-      ierr = VecNormEnd(w,NORM_2,ynorm);CHKERRQ(ierr);
+      ierr = VecNormEnd(y,NORM_2,ynorm);CHKERRQ(ierr);
       if (*gnorm != *gnorm) SETERRQ(PETSC_ERR_FP,"User provided compute function generated a Not-a-Number");
     }
   }
--- python/BuildSystem/config/packages/MPI.py.orig	2009-05-27 09:25:47.000000000 +0200
+++ python/BuildSystem/config/packages/MPI.py	2009-05-27 09:26:12.000000000 +0200
@@ -17,6 +17,7 @@
     self.functions        = ['MPI_Init', 'MPI_Comm_create']
     self.includes         = ['mpi.h']
     liblist_mpich         = [['fmpich2.lib','mpich2.lib'],['fmpich2.lib','mpi.lib'],
+		             ['libmpich.a', 'libpthread.a', 'librt.a'],
                              ['libfmpich.a','libmpich.a', 'libpmpich.a'],
                              ['libmpich.a', 'libpmpich.a'],
                              ['libfmpich.a','libmpich.a', 'libpmpich.a', 'libmpich.a', 'libpmpich.a', 'libpmpich.a'],
