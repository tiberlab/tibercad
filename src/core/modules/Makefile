# $Id$
#

topdir := ../../..

# we have to compile each module
subdirs += excitons dssc common signals
subdirs += thermal drift-diffusion elasticity
subdirs += vff etb 
subdirs += polarizationgrid 
subdirs += dd_generic
#subdirs += interpolation
subdirs += quantumespresso
subdirs += imagereader
#subdirs += negf 
#subdirs += maxwell maxwellabsorption 

include $(topdir)/Make.common

#modules: all

include $(topdir)/Make.rules

