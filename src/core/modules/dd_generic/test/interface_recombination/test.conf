# the checks to perform:

# format is:
# check = filename : variable1(precision) variable2(precision) ...
#
# (default precision is 1e-6)
#
# If you omit any vaiables, then it will just check
# if the file has been generated.
#
# You can define more than one check
#
# examples:

inputfile = pin.tib
outputdir = output

check = sweep_driftdiffusion.dat : anode.current cathode.current 
check = driftdiffusion_Vb_1.7_msh.dat : electronRefEnergy holeRefEnergy electronQFermi holeQFermi
